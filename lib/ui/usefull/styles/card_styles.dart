import 'package:flutter/widgets.dart';

class CardStyles {
  static const List<BoxShadow> basicShadows = [
    const BoxShadow(
      color: const Color.fromRGBO(96, 97, 112, 0.16),
      blurRadius: 2,
      offset: const Offset(0, 0.5),
    ),
    const BoxShadow(
      color: const Color.fromRGBO(40, 41, 61, 0.08),
      blurRadius: 1,
    ),
  ];

}
